.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
.claude
builds
resume-claude.sh
dist/
logs/
# Device backups from backup.sh. Multi-megabyte flash images, and nvs.bin holds
# the node's private identity key — not something to push anywhere.
backups/

# OTA image signing private key — NEVER commit; back this up securely.
ota_signing_key.pem

# Python bytecode generated by the asset-regeneration scripts
# (tools/gen_cjk_font.py import path, tools/gen_pinyin_ime.py).
__pycache__/
*.pyc

# Name,   Type, SubType, Offset,   Size,    Flags
# Dual-slot OTA layout for the 16MB T-LoRa Pager:
#   The LittleFS storage fallback pulled the whole LittleFS/VFS layer into the
#   link, so the 3.125MB slots from partitions.csv no longer fit. Both OTA
#   slots grow to 3.5MB (kept equal so in-field updates still work), NVS and
#   coredump keep their sizes, and the remaining flash becomes a LittleFS
#   partition used when the SD card can't be mounted.
#
#   NOTE: firmware ≥ this table no longer fits the old 3.125MB slots — devices
#   must move to this table via a full USB flash of the factory image (the
#   merged .bin writes it at 0x8000), after which on-device OTA keeps working.
otadata,  data, ota,     0x9000,   0x2000,
app0,     app,  ota_0,   0x10000,  0x380000,
app1,     app,  ota_1,   0x390000, 0x380000,
nvs,      data, nvs,     0x710000, 0x10000,
coredump, data, coredump,0x720000, 0x10000,
littlefs, data, spiffs,  0x730000, 0x8D0000,

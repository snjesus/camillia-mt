[env:tdeck]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 115200

board_build.arduino.memory_type = qio_opi
board_build.flash_size          = 16MB
board_build.partitions          = partitions.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_TDECK=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

# RadioLib raises an advisory #warning on every TU that includes it whenever
# ARDUINO_USB_CDC_ON_BOOT=1 (RadioLib.h:67), telling us to use a hardware UART
# for debug output. USB CDC is the only console these boards expose, so the
# advice cannot be taken and the warning is pure noise -- four copies per build.
#
# build_src_flags, not build_flags: it applies to src/ only, which is where all
# four copies come from (the four of our files that include mesh_radio.h).
# RadioLib's own sources keep full warnings. The cost is that a #warning we add
# in src/ would also be silenced; there are none today.
build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>

[env:tlora-pager-tft]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

board_build.arduino.memory_type = qio_qspi
board_build.flash_size          = 16MB
; app0/app1 geometry identical to partitions.csv; adds a LittleFS partition
; used when the SD card can't be mounted (DM history / export fallback).
board_build.partitions          = partitions_pager_fs.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_TLORA_PAGER_TFT=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
# ~5 MB OTA slots holding the full 9,903-char CJK face — no room for the
# full 776 KB emoji cut too, so 16 MB envs default to the 400-common subset
# (see text_fallback.cpp / emoji_font_compact.h). Cardputer (CJK_FONT_SMALL)
# pairs its smaller CJK cut with the full emoji cut instead.
    -DEMOJI_FONT_COMPACT
# 3.5MB OTA slots + LittleFS-free flash headroom.

# RadioLib raises an advisory #warning on every TU that includes it whenever
# ARDUINO_USB_CDC_ON_BOOT=1 (RadioLib.h:67), telling us to use a hardware UART
# for debug output. USB CDC is the only console these boards expose, so the
# advice cannot be taken and the warning is pure noise -- four copies per build.
#
# build_src_flags, not build_flags: it applies to src/ only, which is where all
# four copies come from (the four of our files that include mesh_radio.h).
# RadioLib's own sources keep full warnings. The cost is that a #warning we add
# in src/ would also be silenced; there are none today.
build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    https://github.com/pschatzmann/arduino-audio-driver/archive/v0.3.0.zip
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>
    +<hal/xl9555.cpp>
    +<hal/tlora_rotary_decoder.cpp>

[env:cardputer-cap]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 1500000

board_build.arduino.memory_type = qio_qspi
board_build.flash_size          = 8MB
# 8 MB flash — the one part in the lineup that cannot hold the ~5 MB OTA slots
# the 16 MB boards grew for the full CJK face. This table doubles the old slot
# size to 0x3C0000 (the most an 8 MB part allows), and the env takes the
# 7,000-character CJK face (cjk_font_small.h) to fit — see text_fallback.cpp.
# NOTE: firmware ≥ this table no longer fits the old 3.125MB slots — devices
# must move to it via a full USB flash of the factory image, after which
# on-device OTA keeps working.
board_build.partitions          = partitions_cardputer.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_CARDPUTER_LORA_HAT=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
# 8 MB part, 3.75MB OTA slots: this env takes the 5,000-character CJK face
# instead of the full 9,903 one, plus the full Noto Emoji cmap (~776 KB).
# Together they land at ~99.1 % of the slot — see text_fallback.cpp for the
# per-env emoji cut policy. Default emoji header (emoji_font.h) is full.
    -DCJK_FONT_SMALL

# RadioLib raises an advisory #warning on every TU that includes it whenever
# ARDUINO_USB_CDC_ON_BOOT=1 (RadioLib.h:67), telling us to use a hardware UART
# for debug output. USB CDC is the only console these boards expose, so the
# advice cannot be taken and the warning is pure noise -- four copies per build.
#
# build_src_flags, not build_flags: it applies to src/ only, which is where all
# four copies come from (the four of our files that include mesh_radio.h).
# RadioLib's own sources keep full warnings. The cost is that a #warning we add
# in src/ would also be silenced; there are none today.
build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

lib_deps =
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    M5Cardputer=https://github.com/m5stack/M5Cardputer
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8

build_src_filter =
    +<main_lvgl.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>

[env:heltec-v4]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

board_build.arduino.memory_type = qio_qspi
# board_upload.* matters as much as board_build.flash_size, for the reason
# spelled out on the mesh-deck env below: the esp32-s3-devkitc-1 board
# definition declares an 8 MB part, and that is the size the bootloader is
# built against. A partition table reaching past 8 MB is rejected before any
# app code runs, and the board resets inside the bootloader with nothing on
# serial but the ROM banner. The old 6.75 MB table hid this.
board_build.flash_size          = 16MB
board_upload.flash_size         = 16MB
board_upload.maximum_size       = 16777216
board_build.partitions          = partitions.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_HELTEC_V4_EXPANSION=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    # NimBLE build options (issue #40). This device is only ever a BLE
    # central -- it connects out to a keyboard and never advertises -- so the
    # peripheral and broadcaster roles are compiled out, and one connection
    # is all a keyboard needs. Both trim flash and, more importantly here,
    # internal DRAM. The log level is dropped from the library's default of 5
    # (verbose) because every level compiled in is another block of format
    # strings in flash for output nobody reads.
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL_DISABLED
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER_DISABLED
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=1
    -DCONFIG_BT_NIMBLE_LOG_LEVEL=2
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

# RadioLib raises an advisory #warning on every TU that includes it whenever
# ARDUINO_USB_CDC_ON_BOOT=1 (RadioLib.h:67), telling us to use a hardware UART
# for debug output. USB CDC is the only console these boards expose, so the
# advice cannot be taken and the warning is pure noise -- four copies per build.
#
# build_src_flags, not build_flags: it applies to src/ only, which is where all
# four copies come from (the four of our files that include mesh_radio.h).
# RadioLib's own sources keep full warnings. The cost is that a #warning we add
# in src/ would also be silenced; there are none today.
build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    adafruit/Adafruit BME280 Library@^2.3.0
    adafruit/Adafruit BMP280 Library@^3.0.0
    adafruit/Adafruit Unified Sensor@^1.1.15
    https://github.com/Quency-D/chsc6x/archive/3b2b6cebf3177b3e2c33d06e07909b0b10159516.zip
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8
    # BLE keyboard host (issue #40). Pinned to the 1.x line on purpose: the
    # 2.x line requires Arduino core 3.x and this platform ships core 2.0.17,
    # so a fresh resolve of "^2" -- which is what pio picks unprompted --
    # does not build. Only BLE-keyboard-enabled envs pull it in.
    h2zero/NimBLE-Arduino@^1.4.3

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>
    +<ble_keyboard.cpp>

[env:heltec-v4-vertical]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

board_build.arduino.memory_type = qio_qspi
# board_upload.* matters as much as board_build.flash_size, for the reason
# spelled out on the mesh-deck env below: the esp32-s3-devkitc-1 board
# definition declares an 8 MB part, and that is the size the bootloader is
# built against. A partition table reaching past 8 MB is rejected before any
# app code runs, and the board resets inside the bootloader with nothing on
# serial but the ROM banner. The old 6.75 MB table hid this.
board_build.flash_size          = 16MB
board_upload.flash_size         = 16MB
board_upload.maximum_size       = 16777216
board_build.partitions          = partitions_16mb_fs.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_HELTEC_V4_EXPANSION=1
    -DDEVICE_UI_VERTICAL=1
    -DHELTEC_COMPACT_SELECTOR=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    # NimBLE build options (issue #40). This device is only ever a BLE
    # central -- it connects out to a keyboard and never advertises -- so the
    # peripheral and broadcaster roles are compiled out, and one connection
    # is all a keyboard needs. Both trim flash and, more importantly here,
    # internal DRAM. The log level is dropped from the library's default of 5
    # (verbose) because every level compiled in is another block of format
    # strings in flash for output nobody reads.
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL_DISABLED
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER_DISABLED
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=1
    -DCONFIG_BT_NIMBLE_LOG_LEVEL=2
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

# RadioLib raises an advisory #warning on every TU that includes it whenever
# ARDUINO_USB_CDC_ON_BOOT=1 (RadioLib.h:67), telling us to use a hardware UART
# for debug output. USB CDC is the only console these boards expose, so the
# advice cannot be taken and the warning is pure noise -- four copies per build.
#
# build_src_flags, not build_flags: it applies to src/ only, which is where all
# four copies come from (the four of our files that include mesh_radio.h).
# RadioLib's own sources keep full warnings. The cost is that a #warning we add
# in src/ would also be silenced; there are none today.
build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    adafruit/Adafruit BME280 Library@^2.3.0
    adafruit/Adafruit BMP280 Library@^3.0.0
    adafruit/Adafruit Unified Sensor@^1.1.15
    https://github.com/Quency-D/chsc6x/archive/3b2b6cebf3177b3e2c33d06e07909b0b10159516.zip
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8
    # BLE keyboard host (issue #40). Pinned to the 1.x line on purpose: the
    # 2.x line requires Arduino core 3.x and this platform ships core 2.0.17,
    # so a fresh resolve of "^2" -- which is what pio picks unprompted --
    # does not build. Only BLE-keyboard-enabled envs pull it in.
    h2zero/NimBLE-Arduino@^1.4.3

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>
    +<ble_keyboard.cpp>

# ── Attaky Mesh Deck 1.0 ──────────────────────────────────────────────────────
# Modular frame: Core ESP32 (S3-WROOM-1U-N16R8) + LoRa/GPS expansion + 48-key
# keyboard + Standard Cell power. See src/hal/hw_mesh_deck.h.
#
# Attaky publishes signal roles per connector position but no F-BTB -> GPIO
# table, so this env used to fail loudly rather than build against a guessed pin
# map. That map is now filled in and cross-checked against a working port, and
# hw_mesh_deck.h defines MESH_DECK_PINS_KNOWN, so the target builds.
[env:mesh-deck]
platform  = espressif32@7.0.1
board     = esp32-s3-devkitc-1
framework = arduino

monitor_speed = 115200
upload_speed  = 1500000

# N16R8: 16 MB quad flash, 8 MB octal PSRAM.
#
# board_upload.* matters as much as board_build.flash_size here: the
# esp32-s3-devkitc-1 board definition declares an 8 MB part, and that is the
# size the bootloader is built against. A partition table reaching past 8 MB is
# then rejected before any app code runs — the device resets inside the
# bootloader, over and over, with no serial output beyond the ROM banner. The
# old 6.44 MB table hid this; partitions_16mb_fs.csv fills the full 16 MB.
board_build.arduino.memory_type = qio_opi
board_build.flash_size          = 16MB
board_upload.flash_size         = 16MB
board_upload.maximum_size       = 16777216

# Do not let esptool restart the board after flashing.
#
# A power-up boot on this hardware is reliable; a warm reset over EN straight
# out of download mode is not — the panel lights, the splash appears, and it
# then hangs until power is removed. Whatever state that leaves behind lives in
# something that survives a CPU reset (the expanders, the panel, PSRAM), and no
# driver-level change moved it.
#
# Leaving the board halted after upload turns a confusing hang into an explicit
# step: flash, then power on. It also gives better logs — with the monitor
# already attached when you power up, the capture starts at the ROM banner
# instead of a second or two into boot.
board_upload.after_reset        = no_reset
board_build.partitions          = partitions_16mb_fs.csv

extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_MESH_DECK=1
    # Bring-up diagnostics: a 1 Hz touch/keyboard status line, per-tap touch
    # coordinates, every key after remapping, and a report for presses on
    # unmapped matrix positions. All off now that touch, both keyboard halves
    # and the symbol key are working. Uncomment to bring them back — the code
    # is still there and costs nothing while the flag is undefined.
    # -DMESH_DECK_TOUCH_TRACE=1
    -DRADIOLIB_EXCLUDE_APRS=1
    # Serial goes to UART0 (GPIO43/44) and out through the board's USB-UART
    # bridge, which is why the device enumerates as /dev/cu.usbserial-*. The
    # other targets here use the S3's native USB CDC and set both to 1; doing
    # that on this board sends every log line to a port that is not connected.
    # Matches the Attaky board definition in the wadamesh port.
    -DARDUINO_USB_CDC_ON_BOOT=0
    -DARDUINO_USB_MODE=0
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

build_unflags =
    -std=gnu++11
    # The esp32-s3-devkitc-1 board definition hard-codes -DARDUINO_USB_MODE=1.
    # This board routes its console to UART0 instead, so build_flags sets 0 —
    # and a macro redefined with a *different* value warns on every translation
    # unit (an identical one would be silent, which is why no other env shows
    # this). Drop the board's value rather than shout past it.
    -DARDUINO_USB_MODE=1

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<aw9523.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>

[env:m9]
# Elecrow ThinkNode M9 — ESP32-S3R8, LR1110 radio, 2.4" ST7789, QWERTY keyboard.
# Pin map lives in src/hal/hw_m9.h; see that file for what was verified on
# hardware versus read off the schematic.
platform  = espressif32@7.0.1
board     = thinknode_m9
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

board_build.arduino.memory_type = qio_opi
board_build.flash_size          = 16MB
board_build.partitions          = partitions.csv

# patch_radiolib_lr11x0: preproduction M9 LR1110s run transceiver firmware
# older than 0x0308, which rejects RadioLib's unconditional
# DriveDiosInSleepMode and turns begin() into -706. See the script.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py
    pre:tools/patch_radiolib_lr11x0.py
    post:tools/m9_upload_erase.py

build_flags =
    -DDEVICE_M9=1
    -DRADIOLIB_EXCLUDE_APRS=1
    # Console is an external UART bridge, NOT native USB-CDC. With CDC on boot
    # the logs go to a ttyACM port this board does not expose and the device
    # looks completely silent.
    -DARDUINO_USB_CDC_ON_BOOT=0
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

# No -Wno-cpp here, unlike the CDC boards: RadioLib's USB-CDC advisory only
# fires when ARDUINO_USB_CDC_ON_BOOT=1, which this board does not set.

build_unflags =
    -std=gnu++11

lib_deps =
    lovyan03/LovyanGFX@^1.1.16
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>

# ── Square ────────────────────────────────────────────────────────────────────
# ESP32-S3, 16 MB flash, 8 MB octal PSRAM. QSPI NV3031B panel, LP5814 I2C
# backlight, GT911 touch, and a PCA9555 expander that gates nearly every rail.
# Text entry uses the on-screen keyboard by default and can optionally merge
# input from a paired BLE keyboard through the same pipeline.
# See src/hal/hw_square.h and issue #56.
[env:square]
platform  = espressif32@7.0.1
board     = square
framework = arduino

monitor_speed = 115200
upload_speed  = 921600

board_build.arduino.memory_type = qio_opi
board_build.flash_size          = 16MB
board_upload.flash_size         = 16MB
board_upload.maximum_size       = 16777216
board_build.partitions          = partitions_16mb_fs.csv

# Regenerates src/release_notes.h from RELEASE_NOTES.md before each build.
extra_scripts =
    pre:tools/gen_release_notes.py
    pre:tools/patch_lgfx_dmadesc.py

build_flags =
    -DDEVICE_SQUARE=1
    -DRADIOLIB_EXCLUDE_APRS=1
    -DARDUINO_USB_CDC_ON_BOOT=1
    -DARDUINO_USB_MODE=1
    -DBOARD_HAS_PSRAM
    -DUI_LVGL_POC=1
    -DLV_CONF_INCLUDE_SIMPLE
    # BLE keyboard host: central-only, one connection, low log level. NimBLE
    # 1.x is required by Arduino core 2.0.17.
    -DCONFIG_BT_NIMBLE_ROLE_PERIPHERAL_DISABLED
    -DCONFIG_BT_NIMBLE_ROLE_BROADCASTER_DISABLED
    -DCONFIG_BT_NIMBLE_MAX_CONNECTIONS=1
    -DCONFIG_BT_NIMBLE_LOG_LEVEL=2
    -Isrc
    -Isrc/hal
    -std=gnu++17
    -Os
    !echo "-DAPP_VERSION=\\\"$(cat VERSION | tr -d '\\n')\\\""
    -DEMOJI_FONT_COMPACT

build_src_flags = -Wno-cpp

build_unflags =
    -std=gnu++11

# LovyanGFX 1.2.27, not the ^1.1.16 every other display env pins: Panel_NV3031B
# does not exist before 1.2.27. PlatformIO gives each environment its own
# libdeps copy, so this upgrade is scoped to this board and the other six are
# untouched -- tools/patch_lgfx_dmadesc.py already relies on that isolation.
lib_deps =
    lovyan03/LovyanGFX@1.2.27
    adafruit/Adafruit ADS1X15@2.6.2
    jgromes/RadioLib@^7.7.1
    mikalhart/TinyGPSPlus@^1.0.3
    rweather/Crypto@^0.4.0
    lvgl/lvgl@9.5.0
    knolleary/PubSubClient@^2.8
    h2zero/NimBLE-Arduino@^1.4.3
    https://github.com/pschatzmann/arduino-audio-driver/archive/v0.3.0.zip

build_src_filter =
    +<main_lvgl.cpp>
    +<vnc_host.cpp>
    +<mqtt_bridge.cpp>
    +<keyboard.cpp>
    +<battery_util.cpp>
    +<text_fallback.cpp>
    +<env_sensor.cpp>
    +<gps.cpp>
    +<los.cpp>
    +<config_io.cpp>
    +<storage.cpp>
    +<base64_util.cpp>
    +<web_config.cpp>
    +<ota_update.cpp>
    +<mesh_radio.cpp>
    +<hal/xl9555.cpp>
    +<hal/square_io.cpp>
    +<mesh_proto.cpp>
    +<wubi_ime.cpp>
    +<channel_mgr.cpp>
    +<dm_mgr.cpp>
    +<node_db.cpp>
    +<live_util.cpp>
    +<live_feed.cpp>
    +<debug_flags.cpp>
    +<ignore_list.cpp>
    +<ble_keyboard.cpp>
